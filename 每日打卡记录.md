# 每日打卡记录

> 规则：**每天结束前必须填一次**。哪怕只学了 30 分钟也要填。
> 填写时间建议固定在睡前，成为习惯。
> 这份文件是我自己的进度账本——三个月后回看，它会告诉我哪里真的走过来了。

---

## 打卡格式（直接复制这段）

```
### Day X · YYYY-MM-DD（周几）
- 计划任务：
- 实际完成：
- 学习时长：
- 代码行数 / 产出文件：
- 卡住的地方：
- 怎么解决的：
- 今天最大的收获：
- 明天的第一件事：
- Git 提交了几次：
```

---

## 打卡记录

<!-- 从下面开始，每天往下加一条 -->

### Day 2 · 2026-09-23（周三）· Git + GitHub 全流程打通 ✅

- 计划任务：装 Git → 配身份 → 首次提交 → 注册 GitHub → 配 SSH → 推上去
- 实际完成：**5 件事全部完成，代码已经在 GitHub 上了**
- 学习时长：约 1.5 小时
- 代码行数 / 产出文件：新增 `.gitignore`（配置文件）；GitHub 仓库 10 个文件
- **首次提交记录**：`61e57b7` · `10 files changed, 2184 insertions(+)`
- **远程仓库**：https://github.com/jia10086-nan/fde-12weeks （Public）

- 卡住的地方：
  1. 在 PyCharm 终端敲 `git --version`，报"无法将git项识别为cmdlet、函数、脚本文件或可运行程序的名称"
  2. `git status` 里中文文件名显示成 `\346\257\217...` 这种乱码样
  3. `git add` 时刷了一屏 `LF will be replaced by CRLF` 警告
  4. **`git push` 报 `Repository not found`** ← 这个最像"死路"
  5. 不清楚仓库要不要勾 Add a README

- 怎么解决的：
  1. 排查三步：Git 装了没（✓ 在 `D:\APP\Git`）→ PATH 配了没（✓ 注册表里有）→ 完整路径能跑吗（✓ `2.55.0`）
     结论：**是 PyCharm 拿的还是旧环境变量** → **完全退出 PyCharm 再重开**就好了
  2. 这是 Git 的 `core.quotepath` 默认设置，**文件名没坏，只是显示方式**。用
     `git config --global core.quotepath false` 关掉就正常了
  3. 这是 `core.autocrlf=true` 在正常工作（把 LF 转成 CRLF）。**是警告，不是错误**，
     提交照样成功。只在文件第一次被 add 时出现，之后不再刷
  4. **跑 `ssh -T git@github.com` 让 GitHub 说出"你是谁"**，发现真实用户名是 `jia10086-nan`，
     而远程地址写成了 `jia10086b-nan`（多了个 b）。改地址 → 立刻推送成功
  5. 三个勾**全都不勾**——本地已经有 `README.md` 和 `.gitignore` 了，GitHub 再生成一份会冲突，
     推送会被拒（`rejected - fetch first`）

- 今天最大的收获：**`Repository not found` 是一句"故意含糊"的报错。**
  GitHub 不区分"仓库不存在"和"你没权限"，防止别人拿它探测私有仓库。
  所以**不能靠这句话猜原因**——要跑 `ssh -T git@github.com` 让 GitHub 自己说"我认为你是谁"。
  **排查的第一原则是：找那个能给出确切答案的动作，而不是在模糊信息上猜。**

- 第二个收获：**环境变量是"程序启动时抄的一份快照"。**
  装完带命令行的工具（Python、Git……），**必须重启所有已经开着的程序**——
  重开终端标签是没用的，因为宿主进程还拿着旧值。

- 第三个收获：**`.gitignore` 是提交前的第一道闸门。** 我的 `.venv` 有 **764 个文件**，
  如果直接 `git add .` 就全提交上去了，而且里面记的是绝对路径，别人 clone 下来根本用不了。
  **一旦提交过，再补规则也来不及**——Git 已经记住它们了。

- 明天的第一件事：进入 Day 3 —— 把项目搬到 D 盘（含虚拟环境重建），然后开始写真正的 Python
- Git 提交了几次：**1 次**（昨天 0 次，今天 1 次）
- 踩坑记录新增：1 条 + 1 条成功记录

> **Day 2 完成 ✅ 第 1 周技术项全部通过（含 PyCharm 文件编码设置）。**
> 下一步待办：**第 7 周前必须开启 Windows 长路径支持**（装向量库要用）。
> 第 1 周验收清单 10 项已过 6 项，剩：3 条提交记录、读懂脚本、改坏再修好。

---

### Day 1 · 2026-09-22（周二）· 环境搭建 + 第一个脚本跑通 ✅

- 计划任务：装独立 Python → PyCharm 配虚拟环境 → 跑通 `week1_text_stats.py`
- 实际完成：**全部完成**。Python 3.14.7 装好、.venv 建好、脚本成功跑出统计报告（退出代码 0）
- 学习时长：约 3 小时
- 代码行数 / 产出文件：0 行（今天在搭环境和跑现成代码）

- 卡住的地方：
  1. `py --version` 报帮助文档（要用 `py -V`）
  2. 装 Python 时提示"应用执行别名"和"长路径支持"
  3. 担心 C 盘空间，想挪到 D 盘
  4. 分不清 Python 和 PyCharm 的区别、为什么要配虚拟环境
  5. 中文菜单找不到 `Run → Edit Configurations`
  6. 改代码时把 `import sys` 误敲成 `import ...`
  7. 打开运行配置窗口，左边是空的

- 怎么解决的：
  1. 改用 `py -V`
  2. 别名按提示处理；**长路径先跳过，记入待办（第 7 周前处理）**
  3. 实测 C 盘剩 216GB，结论不用挪；立了原则"程序位置不重要，数据位置才重要"
  4. 想通了三个层级：**PyCharm 是工作台 → 虚拟环境是每个项目的"库柜" → 基础 Python 是地基**
  5. 要到了中英对照表（`Run`=运行、`Edit Configurations`=编辑配置、`Parameters`=**形参**）
  6. 第 14 行改回 `import sys`，灰色"1 个用法"全部消失
  7. 点 `+` 选 Python，手填名称/路径/形参/工作目录

- 今天最大的收获：**原来我没有那么"学不会"——遇到看不懂的英文提示，我把它原样贴出来问，问题就解决了。这个动作本身就是技术能力。**

- 第二个收获：**搞懂了"参数"是什么。** 脚本=操作手册，参数=今天用在哪个班。同一份 SOP 能用在不同班上，同一个脚本也能读不同文件——**改文件不用改代码，只改形参**。这就是 FDE 天天要干的"参数化"。

- 明天的第一件事：装 Git + 配置身份，然后把项目推上 GitHub

- Git 提交了几次：0（Git 还没装）
- 踩坑记录新增：5 条

> **Day 1 完成。环境主线全部打通。Day 2 进入 Git 与版本管理。**

---

### Day 0 · 2026-09-22（周二）· 准备日
- 计划任务：拿到 12 周完整计划，理解自己的起点
- 实际完成：拿到逐日执行表、资源索引总表、第 1 周环境清单
- 学习时长：0.5 小时（阅读计划）
- 代码行数 / 产出文件：无
- 卡住的地方：暂无
- 怎么解决的：—
- 今天最大的收获：**明白了自己不是"零基础"，是"技术技能退化的资深业务人"**
- 明天的第一件事：关掉 Windows 应用执行别名，装 Python
- Git 提交了几次：0（还没建仓库）

---

<!-- 从 Day 2 开始，每天自己往下加 -->
