# 第 1 周 · 环境搭建清单（PyCharm 版）

> 目标：命令行能调 Python、PyCharm 能跑代码、能提交到 GitHub。
> **顺序不要乱，从上往下做。** 每完成一项在前面打勾。

---

## 第 0 步：先关掉 Windows 的应用执行别名（最重要，先做这个）

Windows 会在你敲 `python` 的时候劫持命令，跳到应用商店。

路径：**设置 → 应用 → 高级应用设置 → 应用执行别名**

把这两个开关**关掉**：
- [ ] `python.exe`
- [ ] `python3.exe`

关完**重开终端**再继续。

---

## 第 1 步：装一个独立的 Python ✅ 已完成

**为什么要装**：你现在终端里的 `python` 是其他软件自带的，不是独立安装的。
独立安装后，PyCharm 才能稳定找到它，以后装库也不会出问题。

- [x] 用 **Python 安装管理器**装好了独立 Python
- [x] 版本：**Python 3.14.7**
- [x] 解释器真实路径（**记住这个，第 2 步要填**）：

```
C:\Users\94278\AppData\Local\Python\pythoncore-3.14-64\python.exe
```

**验证结果**（已通过）：

```bash
py -V
```
```
Python 3.14.7
```

```bash
py -c "print('环境好了')"
```
```
环境好了
```

> ⚠️ **易错点**：是 `py -V`（大写 V、一个短横线），**不是** `py --version`。
> 用 `--version` 会打印一大段帮助文档，看起来像报错其实不是。
> 命令打出帮助文档时，第一反应先怀疑参数写法。

- [x] 再验证 pip：

```bash
py -m pip --version
```

---

## 第 2 步：用 PyCharm 打开项目（不用装 VSCode）✅ 已完成

你桌面已经有 **PyCharm 2026.1.3**，直接用，不用再装 VSCode。
理由：PyCharm 开箱即用、调试功能强、对新手更友好，不用配一堆插件。

### 打开项目

- [x] 打开 PyCharm
- [x] 选 **Open**（不是 New Project）
- [x] 选择目录：`C:\Users\94278\Desktop\FDE学习\fde-12weeks`
- [x] 如果问 "Trust Project"，选 **Trust**

### 配置 Python 解释器（关键一步）

- [x] 菜单 **File → Settings**（或按 `Ctrl + Alt + S`）
- [x] 左侧找到 **Project: fde-12weeks → Python Interpreter**
- [x] 点右上角的齿轮 → **Add Interpreter → Add Local Interpreter**
- [x] 左侧选 **Virtualenv Environment** → **New**
- [x] **Base interpreter** 手动浏览到这个文件：

```
C:\Users\94278\AppData\Local\Python\pythoncore-3.14-64\python.exe
```

- [x] Location 保持默认（建在项目下的 `.venv` 文件夹）
- [x] 点 **OK**，等它建好虚拟环境

> **什么是虚拟环境**：给每个项目一个"独立的库柜"。项目 A 装了什么库，
> 不会影响项目 B。这是工程化的基本习惯，现在养成，以后省无数麻烦。

**结果验证**（已确认）：`.venv\Scripts\python.exe` 存在，
`pyvenv.cfg` 里 `version = 3.14.7`、`home` 指向上面那个基础 Python。

### 改编码设置（防止中文乱码）✅ 已完成

- [x] **File → Settings → Editor → File Encodings**
- [x] 三个下拉框全部选 **UTF-8**：
  - Global Encoding: `UTF-8`
  - Project Encoding: `UTF-8`
  - Default encoding for properties files: `UTF-8`
- [x] 勾上 **Transparent native-to-ascii conversion**

---

## 第 3 步：跑通第一个脚本 ✅ 已完成

- [x] 在 PyCharm 左侧文件树里找到 `week1_text_stats.py`
- [x] 右键 → **Run 'week1_text_stats'**
- [x] 看到"参数不足"的提示（这是正常的）

### 配运行参数（中文版路径）

- [x] 菜单 **运行(R) → 编辑配置(E)...**
- [x] 点 **+** → 选 **Python**（如果左边是空的）
- [x] **名称**：`week1 text stats`
- [x] **脚本路径**：`...\fde-12weeks\week1_text_stats.py`
- [x] **形参**（英文版叫 Parameters）：`samples/sample_sop.txt`
- [x] **工作目录**：`C:\Users\94278\Desktop\FDE学习\fde-12weeks`
- [x] 点 **应用(A)** → **确定**
- [x] 再运行一次

**实际输出（已跑通，退出代码 0）**：

```
============================================
文本文件统计报告
============================================
文件路径：samples/sample_sop.txt
--------------------------------------------
行数（总）         ：55
行数（非空）        ：38
字符数（含空白）     ：881
字符数（去空白）     ：760
============================================
```

### 换文件统计（改文件不改代码）

脚本是"参数化"的，换个文件只需改形参，不用动代码：

| 方式 | 怎么做 |
|------|--------|
| PyCharm 里 | **运行 → 编辑配置** → 改 **形参** → 确定 → Run |
| 命令行 | `.venv\Scripts\python.exe week1_text_stats.py 目标文件.txt` |
| 懒人法 | 命令行敲到 `.py ` 后留空格，把文件**拖进终端**，路径自动填 |

> **这就是"参数化"的价值**：同一份脚本，指哪打哪。

---

## 第 4 步：装 Git ✅ 已完成

- [x] 下载：https://git-scm.com/download/win
- [x] 安装路径改成 **`D:\APP\Git`**（安装向导第 2 页 `Select Destination Location`）
- [x] 装完**完全退出 PyCharm 再重开**，验证：

```bash
git --version
```
```
git version 2.55.0.windows.5
```

> ⚠️ **踩过的坑**：装完 Git，在 PyCharm 终端里敲 `git --version` 报
> "无法将'git'项识别为 cmdlet……"。**不是没装好**，而是 **PyCharm 拿的是它启动那一刻的旧环境变量**。
> **只重开终端标签没用**，必须**完全退出 PyCharm（文件 → 退出，不是点右上角 ×）**再打开。

- [x] 配置身份：

```bash
git config --global user.name "Jia Shengnan"
git config --global user.email "942785951@qq.com"
```

> ⚠️ 这个邮箱**必须和 GitHub 账号邮箱一致**，否则 GitHub 认不出提交是你做的。

- [x] **关掉换行符警告**（Windows 特有）：

```bash
git config --global core.autocrlf true
```

- [x] **关掉中文文件名转义显示**（否则 `git status` 里中文变成 `\346\257\217...`）：

```bash
git config --global core.quotepath false
```

- [x] 验证：

```bash
git config --global --list
```
```
safe.directory=*
user.name=Jia Shengnan
user.email=942785951@qq.com
core.autocrlf=true
core.quotepath=false
```

> `safe.directory=*` 不是我们配的，是某个工具自动写进去的（意思是"信任本机所有目录"）。
> 个人单机使用没有影响，**别动它**。

---

## 第 5 步：注册 GitHub 并配好 SSH 密钥 ✅ 已完成

### 注册信息（记下来，以后常用）

| 项目 | 值 |
|------|-----|
| 用户名 | **`jia10086-nan`** |
| 邮箱 | `942785951@qq.com`（和 Git 配的一致 ✅） |
| 主页 | https://github.com/jia10086-nan |

### SSH 密钥（已自动生成，无需再跑 `ssh-keygen`）

```
C:\Users\94278\.ssh\id_ed25519       ← 私钥（留在本机，永远不给任何人）
C:\Users\94278\.ssh\id_ed25519.pub   ← 公钥（已贴到 GitHub）
```

公钥内容（贴到 GitHub 用的就是这一行）：

```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBGIEKZXoFVx61XbIqd5XVFRN74uettUzt1FsdNTiGOA 942785951@qq.com
```

> **公钥是锁，私钥是钥匙。** 公钥可以随便给人，GitHub 拿它认你；
> 私钥一旦泄漏，别人就能冒充你推代码。**私钥永远不要复制到任何地方。**

- [x] 粘贴到 GitHub：**头像 → Settings → SSH and GPG keys → New SSH key**
- [x] 验证：

```bash
ssh -T git@github.com
```
```
Hi jia10086-nan! You've successfully authenticated, but GitHub does not provide shell access.
```

> **退出码是 1 也没关系**——GitHub 不提供 shell，所以它故意返回非 0。
> 只要看到 `Hi 用户名!` 这一句，就说明成功了。
>
> 💡 **这句话的真正用途**：它会告诉你 **GitHub 认为你是谁**。
> 遇到"推送找不到仓库"时，第一件事就是跑它——**先确认用户名，再查仓库名**。

> **如果超时或卡住**：那是网络问题，不是配置问题。改用 Gitee（码云），流程完全一样。

---

## 第 6 步：把代码推上去 ✅ 已完成

### 仓库信息

| 项目 | 值 |
|------|-----|
| 仓库地址（网页） | https://github.com/jia10086-nan/fde-12weeks |
| 仓库地址（SSH） | `git@github.com:jia10086-nan/fde-12weeks.git` |
| 可见性 | **Public**（选它的理由：这是作品集的第一块砖，面试时可以直接发链接） |

- [x] GitHub 网页点 **Create repository**
  - 名字：`fde-12weeks`
  - ⚠️ **三个初始化选项全部不勾**（Add a README / Add .gitignore / Choose a license）

> ⚠️ **为什么三个勾都不能勾**：本地仓库**已经有** `README.md` 和 `.gitignore` 了。
> 如果 GitHub 上再生成一份，两边内容对不上，推送会被**直接拒绝**，报
> `rejected - fetch first`。**新手在这里卡半小时的特别多。**

- [x] 本地依次执行：

```bash
git init
git add .
git commit -m "第1周：环境搭建 + 文本统计脚本"
git branch -M main
git remote add origin git@github.com:jia10086-nan/fde-12weeks.git
git push -u origin main
```

**实际输出（已成功）**：

```
Enumerating objects: 13, done.
...
To github.com:jia10086-nan/fde-12weeks.git
 * [new branch]      main -> main
branch 'main' set up to track 'origin/main'.
```

- [x] 刷新 GitHub 网页，能看到 10 个文件 ✅
- [x] 远端确认：`git ls-remote --heads origin` → `61e57b7... refs/heads/main`

> **`-u` 是什么意思**：`--set-upstream`，把本地 `main` 和远程 `origin/main` **绑定**。
> 绑过这一次之后，以后推送只要敲 `git push` 就够了，不用再写 `origin main`。

> **`master` 为什么改成 `main`**：老版本 Git 默认叫 `master`，GitHub 现在默认叫 `main`。
> 不改的话，本地推上去会多出一个 `master` 分支，而 GitHub 首页那个空着的 `main` 一直空着，很别扭。
> `git branch -M main` 的 `-M` 是"强制改名"（`-m` 是小写版，已存在同名分支时会报错）。

---

## 验证清单（全部打勾才算第 1 周完成）

**技术项（已全部通过 ✅）**

- [x] `py -V` 输出 `Python 3.14.7`（独立安装的 Python）
- [x] PyCharm 能打开项目，解释器配的是虚拟环境 `.venv`（基于 Python 3.14.7）
- [x] PyCharm 里能跑通 `week1_text_stats.py`，看到统计报告（退出代码 0）
- [x] `git --version` → `git version 2.55.0.windows.5`
- [x] `git config --global --list` 能看到用户名、邮箱、autocrlf、quotepath
- [x] `ssh -T git@github.com` → `Hi jia10086-nan! You've successfully authenticated`
- [x] GitHub 网页上能看到仓库和 10 个文件
- [x] `git log` 有 1 条提交记录（`61e57b7`）
- [x] 远端已确认：`refs/heads/main` = 本地 `61e57b7`，完全一致

**认知项（自己心里过一遍）**

- [x] 编码设置为 UTF-8（文件 → 设置 → 编辑器 → 文件编码，三项全选 UTF-8 + Transparent native-to-ascii conversion）
- [ ] 能说清 `git add` / `git commit` / `git push` 三者的区别
      → 答得出这 4 句就算过：
      ① `add` = 把改动放进"暂存区"（选哪些要提交）
      ② `commit` = 把暂存区存成一个快照，**存在你电脑上**
      ③ `push` = 把本地快照**送到 GitHub**
      ④ 少了任何一步都不行——**commit 了不代表 GitHub 上有**

---

## 中文版菜单对照表（PyCharm 中文化后必备）

| 英文 | 中文 |
|------|------|
| Run | **运行** |
| Debug | **调试** |
| Edit Configurations... | **编辑配置(E)...** |
| Parameters | **形参** |
| Script path | **脚本路径** |
| Working directory | **工作目录** |
| Environment variables | **环境变量** |
| Apply | **应用(A)** |
| OK | **确定** |
| File → Settings | **文件 → 设置** |

> 💡 遇到英文教程对不上菜单时，**把鼠标悬停在菜单项上**，通常会浮出英文原名。

---

## 待办：长路径支持（第 7 周前必须处理）

安装 Python 时提示过长路径支持，当时选了 **N**。现在没问题，
但**第 7 周装向量库（chromadb / faiss）之前必须打开**，否则某些包会装不上。

处理方式：`Win + R` → `gpedit.msc` → 计算机配置 → 管理模板 → 系统 → 文件系统
→ **启用 Win32 长路径** → 启用 → **重启电脑**。

> 已记入 `踩坑记录.md`，第 6 周末我会主动提醒。

---

## PyCharm 常用快捷键（先记这 6 个）

| 快捷键 | 作用 |
|--------|------|
| `Shift + F10` | 运行当前文件 |
| `Ctrl + /` | 注释/取消注释选中行 |
| `Ctrl + Alt + S` | 打开设置 |
| `Ctrl + B` | 跳到函数/变量的定义处 |
| `Alt + Enter` | 快速修复（PyCharm 建议怎么改） |
| `Tab` | 代码补全确认 |

> **`Ctrl + B` 和 `Alt + Enter` 这两个最值得练**。
> `Ctrl + B` 让你能"跳进去看别人怎么写的"，`Alt + Enter` 是 PyCharm 主动告诉你哪里能改好。
> **这两个键用熟了，你就有了一个随时在旁边的老师。**

---

## 常见坑速查

| 现象 | 原因 | 解决 |
|------|------|------|
| 敲 `python` 跳应用商店 | 应用执行别名劫持 | 见第 0 步 |
| `py` 命令找不到 | Python 没装成功 | 重跑 Python 安装管理器 |
| `py --version` 打印一大段帮助 | 参数写法不对 | 用 `py -V`（大写 V） |
| `'python' 不是内部或外部命令` | PATH 没配好 | 重开终端；还不行就重装 |
| PyCharm 找不到 Python 3.14.7 | 没手动浏览路径 | 填 `C:\Users\94278\AppData\Local\Python\pythoncore-3.14-64\python.exe` |
| PyCharm 找不到解释器 | 没配虚拟环境 | 见第 2 步的"配置 Python 解释器" |
| 中文输出乱码（像 `æ–‡ä»¶`） | 编码不是 UTF-8 | 见第 2 步的编码设置 |
| 脚本报"参数不足" | 没传文件路径 | 见第 3 步的 Run Configuration |
| `please tell me who you are` | Git 没配身份 | 见第 4 步 |
| 每次提交刷 `LF will be replaced by CRLF` | Windows 换行符差异 | `git config --global core.autocrlf true` |
| `git push` 卡住不动 | 网络问题 | 换 Gitee，或用代理 |
| `Permission denied (publickey)` | SSH 密钥没配好 | 重做第 5 步 |

---

## 记录你的坑

每遇到一个问题，去 `踩坑记录.md` 里记一条。
**你踩过的坑就是你最值钱的笔记**——三个月后你一定会忘，但文件不会。
