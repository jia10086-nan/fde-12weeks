# 踩坑记录

> 规则：每遇到一个问题，立刻在这里记一条。不问大小，不限格式。
> 这份文件在整个 12 周里会一直用，一周整理一次。

## 记录格式

```
### YYYY-MM-DD 一句话描述问题
- 现象：
- 报错原文：
- 试过什么：
- 最后怎么解决的：
- 下次怎么避免：
```

---

## 示例（可以删）

### 2026-09-22 命令行输入 python 没反应，跳出了应用商店
- 现象：在 PowerShell 里敲 `python`，没进解释器，反而打开了微软应用商店
- 报错原文：（无报错，直接跳转应用商店）
- 试过什么：重启终端、重装 Python
- 最后怎么解决的：这是 Windows 自带的"应用执行别名"在劫持。到「设置 → 应用 → 高级应用设置 → 应用执行别名」里，把 `python.exe` 和 `python3.exe` 两个开关关掉，再重开终端。
- 下次怎么避免：装完 Python 第一件事就是关这两个别名。

---

## 我的记录

### 2026-09-22 `py --version` 报了一堆帮助文档，不是版本号
- 现象：敲 `py --version`，出来的是使用方法说明，不是版本号
- 报错原文：`Usage: py <regular Python options> ...`
- 试过什么：反复重跑、怀疑没装好
- 最后怎么解决的：改用 `py -V`（大写 V，单个短横线）就正常输出 `Python 3.14.7`
- 下次怎么避免：**命令报帮助文档，第一反应先怀疑"参数写法不对"，而不是"我搞错了"**。查用法用 `py help`

### 2026-09-22 装 Python 时提示"应用执行别名"和"长路径支持"
- 现象：运行 Python 安装管理器，弹出两段英文提示要配置
- 最后怎么解决的：
  - 应用执行别名 → 去「设置 → 应用 → 高级应用设置 → 应用执行别名」，确保 Python 相关项是开启的
  - 长路径支持（>260字符）→ 当时选了 N，**未处理**
- 待办：⚠️ **第 7 周装向量库前，需要开启长路径支持**（否则某些包会装不上）。
  开启方式：注册表/组策略改 Win32 长路径，或重新运行 Python 安装管理器时选 Y。需要重启电脑。
- 下次怎么避免：装开发环境时，这类"要不要开某功能"的提示，**先想"后面会不会用到"再决定**，不确定就开着

### 2026-09-22 误按 Ctrl+A
- 现象：终端里出现 `^A^A`
- 原因：`Ctrl+A` 在终端是"跳到行首"，不输入任何字符
- 解决：忽略即可，直接回车
- 下次怎么避免：在终端里 `Ctrl+A` 不是"全选"，别按

### 2026-09-22 C 盘空间顾虑（结论：不用挪）
- 现象：担心 Python 和 PyCharm 装在 C 盘占空间，想挪到 D 盘
- 实测数据：C 盘剩余 **216.3GB**、D 盘剩余 485.3GB；
  Python 本体仅 **166MB**、PyCharm 本体 **3.3GB**、pip 缓存 67MB
- 结论：**没必要挪**。216GB 空余足够装十几次完整环境，而挪 Python 位置会破坏 PATH
  和注册表，风险远大于收益
- 正确做法（原则：**程序位置不重要，数据位置才重要**）：
  1. 项目文件放 D 盘（第 1 周验收后搬）
  2. pip 缓存迁到 D 盘：`pip config set global.cache-dir D:\pip-cache`
  3. 以后新建项目的虚拟环境放 D 盘

### 2026-09-22 菜单栏是中文的，英文教程对不上
- 现象：跟着英文教程找 `Run → Edit Configurations`，但 PyCharm 界面是中文
- 关键对照：
  - `Run` = **运行**
  - `Edit Configurations` = **编辑配置**
  - `Parameters` = **形参**（⚠️ 中文版不叫"参数"，就叫"形参"）
  - `Script path` = **脚本路径**
  - `Apply` = **应用** / `OK` = **确定**
- 下次怎么避免：**鼠标悬停在菜单项上**，PyCharm 通常会浮出英文原名。或者直接问 AI 要中英对照表

### 2026-09-22 修改脚本时误把 `import sys` 敲成 `import ...`
- 现象：编辑器里第 14 行变成 `import ...`，后面所有函数都挂着灰色"1 个用法"
- 报错原文：`SyntaxError: invalid syntax`
- 试过什么：看不懂那些灰色"1 个用法"是什么意思
- 最后怎么解决的：把第 14 行改回 `import sys`，灰色提示全部消失
- 下次怎么避免：**那些灰色的"1 个用法"不是错误信息，而是 PyCharm 在说"这个函数还没被用到"。**
  如果满屏都是这个提示，**第一反应去查文件顶部有没有语法错误**——一处语法错会让整个文件解析失败

### 2026-09-22 运行配置窗口左边是空的
- 现象：打开「运行 → 编辑配置」，左边列表显示"未添加运行配置"
- 原因：之前的 Run 是**临时运行**，PyCharm 没有把配置存下来
- 最后怎么解决的：点 **+** → 选 **Python** → 手动填名称/脚本路径/形参/工作目录。
  或者**先右键 Run 一次**，配置就会自动被保存进列表
- 下次怎么避免：想要配置持久保留，就得先创建一个正式配置，别指望临时 Run 会留下记录

### 2026-09-22 ✅ 成功跑通第一个脚本（记录一下，这条不是坑）
- 运行命令：`.venv\Scripts\python.exe week1_text_stats.py samples/sample_sop.txt`
- 输出：行数（总）55 / 行数（非空）38 / 字符数（含空白）881 / 字符数（去空白）760
- 标志：**退出代码为 0**（之前报参数不足时是 1）
- 关键认知：**改文件不用改代码，只改"形参"就行** —— 这就是"参数化"的价值

### 2026-09-23 装完 Git，PyCharm 终端里敲 git 却说"无法识别"
- 现象：在 PyCharm 内置终端敲 `git --version`，报
  `无法将"git"项识别为 cmdlet、函数、脚本文件或可运行程序的名称`
- 排查过程（一步步排除）：
  1. Git 到底装了没？→ `D:\APP\Git\cmd\git.exe` 存在 ✓
  2. 系统 PATH 里有吗？→ 注册表里已有 `D:\APP\Git\cmd` ✓
  3. 用完整路径实测能不能跑？→ `git version 2.55.0.windows.5` 正常 ✓
  4. 所以：**Git 一点问题没有，是这个终端的环境变量没刷新**
- 最后怎么解决的：**完全退出 PyCharm（菜单「文件 → 退出」，不是点右上角关闭窗口），再重新打开**，
  终端里敲就正常了
- 下次怎么避免：
  - **每个程序在启动时会"抄一份"环境变量，之后就再也不看系统了。**
    所以装完带命令行的工具（Python、Git、Node……），**必须重启所有已经开着的程序**。
  - **只重开终端标签是没用的**——PyCharm 进程本身还拿着旧的环境变量。
  - 判断技巧：**用完整路径试一次**。完整路径能跑通 = 程序没问题，就是环境变量的事。


