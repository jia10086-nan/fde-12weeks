# ============================================
# .gitignore —— 告诉 Git：这些文件不要提交
# ============================================
# 每一行是一个规则。匹配到的文件，Git 会完全忽略它。
# 规则越早写越好——一旦文件被提交过，再写就来不及了。

# 【最重要】虚拟环境
# .venv 里有 700 多个文件、约 10MB，而且里面记的是你电脑的绝对路径。
# 别人 clone 下来根本用不了，必须自己重建一个。所以绝对不能提交。
.venv/
venv/
env/

# Python 自动生成的缓存文件（每次运行都会变，没必要提交）
__pycache__/
*.py[cod]
*.egg-info/

# PyCharm 的个人配置（换台电脑就不一样，不该共享）
.idea/

# Windows 系统自动生成的垃圾文件
Thumbs.db
Desktop.ini

# 【安全】密钥类文件 —— 永远不要提交到网上
# 以后接大模型 API 时，你的密钥就放在这类文件里，泄漏了会被盗刷。
.env
.env.local
*.key
*.pem
