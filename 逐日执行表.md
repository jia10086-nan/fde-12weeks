# FDE 12 周学习计划 · 逐日执行表

> **定位**：这是我自己贴在桌前用的执行手册，不用于汇报、不用于展示。
> **前置假设**：我按零基础执行——即使有些东西我"学过但忘了"，也按"从零唤回"的节奏走，不跳步。
> **每天 2-3 小时**。看视频/读文档不超过 40 分钟，其余全部在键盘上。

---

## 使用说明（先读这 5 条）

1. **每天只做当天的任务**。不要提前翻后面的，也不要回头看前面的——除非验收没过。
2. **完成的定义是"代码跑起来"**，不是"视频看完了"。看完了但没写出来 = 没完成。
3. **卡住不超过 40 分钟**。超过就记进 `踩坑记录.md`，然后向下走或问我。
4. **每天结束必须提交 Git**，哪怕只改了一行。提交信息写清楚做了什么。
5. **每周日做周验收**。验收不过，下一周不开始新内容，先补。

---

## 第 1 周 · 环境能跑，第一个脚本跑通

**本周唯一目标**：让开发环境完整可用，并完成"写→跑→改→提交"的第一个完整闭环。

### Day 1（周一）· 装 Python，跑通命令行

| 项 | 内容 |
|---|---|
| 任务 | 关掉 Windows 应用执行别名 → 装 Python → 验证 `python --version` 和 `pip --version` |
| 产出 | 终端里能正常输出 Python 版本号 |
| 参考 | `第1周环境搭建清单.md` 第 0-1 步 |
| 卡点预警 | 敲 `python` 跳到应用商店 = 别名没关；提示"不是内部或外部命令" = 装的时候没勾 Add to PATH |

**今日验收**：把 `python --version` 的输出截图或抄进打卡记录。版本号必须 ≥ 3.12。

---

### Day 2（周二）· 装编辑器和 Git

| 项 | 内容 |
|---|---|
| 任务 | 装 VSCode + Python/Pylance 扩展 → 改编码设置 → 装 Git → 配用户名邮箱 → 配换行符 |
| 产出 | `git --version` 有输出；`git config --global --list` 能看到自己的名字和邮箱 |
| 参考 | 环境清单第 2-3 步 |
| 卡点预警 | 中文乱码 = 编码设置没改；`please tell me who you are` = 身份没配 |

**今日验收**：`git config --global --list` 输出里必须有 `user.name` 和 `user.email`。

---

### Day 3（周三）· GitHub 账号与密钥

| 项 | 内容 |
|---|---|
| 任务 | 注册 GitHub → 生成 SSH 密钥 → 加到 GitHub → 验证 `ssh -T git@github.com` |
| 产出 | 看到 `Hi 你的用户名! You've successfully authenticated` |
| 参考 | 环境清单第 4 步 |
| 卡点预警 | **网络问题**。如果超过 40 分钟连不上，直接改用 Gitee（码云），流程完全一样。**不要在这件事上耗掉一天。** |

**今日验收**：`ssh -T git@github.com` 成功，或已在 Gitee 完成等效操作。

---

### Day 4（周四）· 建仓库，推第一个文件

| 项 | 内容 |
|---|---|
| 任务 | GitHub 建私有仓库 `fde-12weeks` → 本地 `git init` → 放入 README → 提交 → 推送 |
| 产出 | GitHub 网页上能看到仓库和文件 |
| 参考 | 环境清单第 5 步 |
| 关键 | **必须理解 `git add` / `git commit` / `git push` 三者的区别**。不理解就去查，这是后面所有工作的基础 |

**今日验收**：能用自己的话说清：`add` 是把改动放进待提交区、`commit` 是存成一个版本快照、`push` 是传到远程仓库。

---

### Day 5（周五）· 跑通第一个脚本

| 项 | 内容 |
|---|---|
| 任务 | 把 `week1_text_stats.py` 放进目录 → 用 `samples/sample_sop.txt` 跑通 → 看懂它在干什么 |
| 产出 | 终端输出统计报告 |
| 命令 | `python week1_text_stats.py samples/sample_sop.txt` |
| 关键 | **逐行读懂这个脚本**。它只有 5 个函数，每个函数干什么、为什么这么拆，都要能说出来 |

**今日验收**：指着脚本里的任意一行，能说出它在做什么。

---

### Day 6（周六）· 改坏它，再修好它 ⭐ 本周最重要的一天

| 项 | 内容 |
|---|---|
| 任务 | ①删掉 `encoding="utf-8"`，跑一次，观察现象 ②改回来 ③把"字符数"改成"词数"（按空格切分统计） ④加一个"平均每行字符数"的新指标 |
| 产出 | 修改后的脚本能跑，输出多了新指标 |
| 为什么重要 | **这一步的意义不是学语法，是建立"代码是我写的、我知道改哪、坏哪、怎么救"的控制感**。这是"会写代码"最原始的内核 |
| 卡点预警 | 报错看不懂就搜报错原文。这一步的报错会是很典型的 `UnicodeDecodeError`，搜索引擎一搜就有 |

**今日验收**：新脚本能跑通并输出 5 个指标（行数、非空行、字符含空白、字符去空白、平均每行字符）。

---

### Day 7（周日）· 提交 + 周验收

| 项 | 内容 |
|---|---|
| 任务 | 提交代码 → 做周验收 → 填周复盘 |
| 命令 | `git add .` → `git commit -m "第1周完成：环境搭建+文本统计脚本"` → `git push` |

**本周验收清单（全过才算完成）**

- [ ] 终端 `python --version` 正常，不跳应用商店
- [ ] `git --version` 正常
- [ ] `ssh -T git@github.com` 通过（或 Gitee 等效）
- [ ] GitHub 网页能看到仓库
- [ ] 本地能跑通 `week1_text_stats.py`
- [ ] `git log` 至少有 **3 条**提交记录
- [ ] 能用自己的话说清 `git add`/`commit`/`push` 的区别
- [ ] 能指着脚本任意一行说出它在干什么
- [ ] 改坏了脚本一次，并且自己修好了
- [ ] `踩坑记录.md` 里至少有 2 条记录

---

## 第 2 周 · Python 工程化：写第一个"有用"的工具

**本周目标**：从"玩具脚本"进到"真的会用的工具"——写一个日报收集器。
**本周资源**：遇到不懂的语法，查咕泡「10 Python 快速入门」对应集，或 B站小甲鱼/黑马相应章节。**不顺序看。**

### Day 8（周一）· 函数与数据结构复习（边用边学）

| 项 | 内容 |
|---|---|
| 任务 | 搞懂四件事：函数怎么定义和调用、返回值怎么用、字典和列表怎么嵌套、循环怎么遍历嵌套结构 |
| 动作 | **不要看教程**。打开编辑器，自己写 5 个小练习（见下），卡住了再搜 |
| 练习 | ①写函数求列表最大值 ②写函数把字典的键值对调换位置 ③遍历一个"列表套字典"的结构并打印 ④写函数统计一段话里每个字出现的次数 ⑤写函数判断字符串是不是纯数字 |
| 产出 | `week2_practice.py`，5 个函数都能跑 |

**今日验收**：5 个练习都能独立跑通，且你能解释每个函数的输入和输出。

---

### Day 9（周二）· 文件读写：JSON 与 CSV

| 项 | 内容 |
|---|---|
| 任务 | 学会用 Python 读写 JSON 和 CSV 文件。**这是日报收集器的地基** |
| 动作 | ①把一份日报数据存成 JSON 再读回来 ②存成 CSV 再读回来 ③对比两种格式的差别（什么时候用哪个） |
| 难点 | 编码问题（中文必须用 `encoding="utf-8"`）；JSON 的 `dumps`/`loads` 和 `dump`/`load` 的区别 |
| 产出 | `week2_file_io.py`，能完成 JSON 和 CSV 的读写往返 |

**今日验收**：写进去的数据能原样读出来，中文不乱码。

---

### Day 10（周三）· 日报数据建模 + 校验逻辑

| 项 | 内容 |
|---|---|
| 任务 | 定义日报的数据结构，写校验函数 |
| 数据字段 | 今日工作（按时序）、工作时长、遇到问题、明日计划 |
| 动作 | ①设计每条日报用字典怎么表示 ②写函数校验一条日报：四个字段是否齐全？工作时长是否是数字？内容是否为空？ ③返回校验结果（合格/不合格 + 缺什么） |
| 产出 | `week2_validator.py`，能对一条日报给出明确的校验结论 |

**今日验收**：故意传入一条缺"明日计划"的日报，函数能准确指出缺的是哪项。

---

### Day 11（周四）· 批量处理与异常处理

| 项 | 内容 |
|---|---|
| 任务 | 从"处理一条"进到"处理一批"，并且不崩 |
| 动作 | ①准备一份含 10 条日报的 JSON 数据（其中混入 2 条格式错误的） ②写主程序遍历全部，输出合规/不合规的统计 ③**用 try/except 包住每条的处理**，保证一条坏了不影响其他条 |
| 关键 | 异常处理是本周的分水岭。"跑通就行"和"工程化"的区别就在这里 |
| 产出 | `week2_daily_report.py`，能处理一批数据并输出汇总 |

**今日验收**：传入含错误数据的文件，程序**不崩溃**，且明确指出哪几条有问题、问题是什么。

---

### Day 12（周五）· 调试能力专项 ⭐

| 项 | 内容 |
|---|---|
| 任务 | 专门练"读报错 + 定位问题" |
| 动作 | ①故意制造 5 种典型错误（变量名拼错、类型不匹配、索引越界、除以零、文件不存在）②每次先**读报错信息的最后一行**，猜问题在哪，再去看代码 ③用 VSCode 断点功能单步执行一次程序 |
| 为什么专项练 | **很多人写不出代码不是不会写，是不会修。报错看不懂就放弃了。** |
| 产出 | `week2_errors.md`，记录 5 种错误的报错原文 + 原因 + 解法 |

**今日验收**：5 种错误都能自己定位并修好；会用断点看变量的值。

---

### Day 13（周六）· 完整跑通 + 输出汇总

| 项 | 内容 |
|---|---|
| 任务 | 把前几天的模块合起来，形成一个能用的日报收集器 |
| 动作 | ①读取 → ②校验 → ③汇总 → ④输出一份格式正确的汇总文件（txt 或 csv） |
| 产出 | 一份 `report_summary.txt`，含总条数、合规数、不合规数、不合规明细 |

**今日验收**：一条命令跑完全流程，输出文件内容正确。

---

### Day 14（周日）· 提交 + 周验收

**本周验收清单**

- [ ] `week2_practice.py` 5 个练习都能跑
- [ ] 能读写 JSON 和 CSV，中文不乱码
- [ ] 校验函数能准确指出缺哪个字段
- [ ] 主程序处理含错误数据时**不崩溃**
- [ ] `week2_errors.md` 里记录了 5 种错误的报错与解法
- [ ] 会用 VSCode 断点
- [ ] 汇总文件输出格式正确
- [ ] `git log` 本周至少 4 条提交

---

## 第 3 周 · 把工具做成"别人也能用"

**本周目标**：解决"交付"问题——路径不写死、代码分模块、有 README、别人能跑通。
**这一周结束，原路线阶段 1 的"编程语言"与"数据能力"两项补齐。**

### Day 15（周一）· 命令行参数

| 项 | 内容 |
|---|---|
| 任务 | 把写死的路径改成命令行参数 |
| 动作 | 用 `sys.argv` 或 `argparse`，让用户可以指定输入文件路径和输出目录 |
| 为什么重要 | **这是"可交付"和"自用玩具"的分界线** |
| 产出 | `python week3_report.py --input data.json --output ./out` 能跑 |

**今日验收**：参数写错时给出友好提示，不是一堆堆栈。

---

### Day 16（周二）· 模块化拆分（上）

| 项 | 内容 |
|---|---|
| 任务 | 把单文件拆成多文件，理解"为什么要分模块" |
| 动作 | 拆成三个文件：`data_loader.py`（读取）、`validator.py`（校验）、`main.py`（串联） |
| 关键概念 | **`import` 怎么用**；**为什么要分**——不是为了好看，是为了改一处不牵连全局 |
| 产出 | 目录结构清晰的多文件项目，`python main.py` 仍能跑通 |

**今日验收**：能说出"如果我要改校验规则，应该改哪个文件、为什么不用动其他文件"。

---

### Day 17（周三）· 模块化拆分（下）+ 配置分离

| 项 | 内容 |
|---|---|
| 任务 | 把"配置"从代码里抽出来 |
| 动作 | ①把字段名、输出格式等配置抽到一个 `config.py` 或 JSON 配置 ②让代码从配置读，而不是硬编码 ③加一个 `utils.py` 放公共函数（如格式化输出） |
| 产出 | 改配置就能改行为，不用改代码 |

**今日验收**：改配置文件的字段名，程序输出随之变化，代码一行未动。

---

### Day 18（周四）· 写 README

| 项 | 内容 |
|---|---|
| 任务 | 给工具写一份让不懂代码的人也能看懂的 README |
| 必须包含 | ①这个工具干什么用 ②怎么装依赖（要列出具体依赖） ③怎么运行（给出完整命令示例） ④输入输出是什么格式 ⑤常见问题 |
| 产出 | `README.md` 完整版 |

**今日验收**：README 里每一条命令都是**你自己跑过的**，不是抄的。

---

### Day 19（周五）· 找真人测试 ⭐

| 项 | 内容 |
|---|---|
| 任务 | 叫一个不写代码的同事，照你的 README 跑一遍 |
| 记录 | 他在哪一步卡住了？他问了什么问题？哪儿需要你口头补充？ |
| 关键 | **跑不通的地方就是 README 需要改的地方。** 这一条是真实的 FDE 式交付训练 |
| 产出 | 根据测试结果修订的 README |

**今日验收**：有至少一位真人照 README 跑通，或你根据他的卡点改好了 README。

---

### Day 20（周六）· 补 SQL 基础（第一块）

| 项 | 内容 |
|---|---|
| 任务 | **SQL 是 FDE 的硬性最低门槛，但咕泡课程里没有**。开始补 |
| 动作 | ①装一个本地数据库（SQLite 最简单，Python 自带） ②学会 `SELECT` / `WHERE` / `ORDER BY` / `LIMIT` ③用自己的日报数据建一张表，把数据插进去 |
| 资源 | B站搜"SQL 入门"，或咕泡「11 Python 数据科学」里的 pandas 读数据库部分 |
| 产出 | `week3_sql.py`，能用 Python 连 SQLite 并查数据 |

**今日验收**：能用自己的话解释 `SELECT ... FROM ... WHERE ... ORDER BY ... LIMIT` 每个子句在做什么。

---

### Day 21（周日）· 提交 + 周验收 + 阶段总结

**本周验收清单**

- [ ] 能通过命令行参数控制输入输出
- [ ] 代码拆成 ≥3 个模块，职责清晰
- [ ] 有配置文件，改配置不改代码
- [ ] README 完整，同事能照它跑通
- [ ] 会基本的 SQL 查询（SELECT/WHERE/ORDER BY）
- [ ] GitHub 仓库有清晰的提交历史（不是一次性全提交）

**阶段一总验收（第 1-3 周）**

用 `week3` 的工具，在 30 分钟内完成一次"从陌生到跑通"的演示。做不到就补，不要进第 4 周。

---

## 第 4 周 · 第一次亲手调通大模型接口

**本周目标**：理解"大模型对程序来说就是一个网络接口"，并亲手调通。
**本周资源**：咕泡「10 Python 快速入门」相关集 + B站「吴恩达 提示词工程」BV1173jzNELG

### Day 22（周一）· 概念建立 + 注册模型服务

| 项 | 内容 |
|---|---|
| 任务 | ①理解一个核心概念：**大模型就是一个网络接口，输入文本、返回文本，仅此而已** ②注册一个模型服务账号，拿到密钥 |
| 建议 | 至少准备国内和国外各一个（价格与可用性不同，场景不同各有所长） |
| 产出 | 拿到 API Key，能调用服务的 API 文档页面找到 |

**今日验收**：能用一句话说明"调用大模型"和"调用其他网络接口"本质上没有区别。

---

### Day 23（周二）· 发出第一个请求

| 项 | 内容 |
|---|---|
| 任务 | 用 Python 发一次请求，把返回结果打印出来 |
| 动作 | ①装 SDK 或直接用 `requests` ②写一个最小脚本：发送"你好" ③打印返回的完整结构，**看清里面有什么** |
| 产出 | `week4_first_call.py` |

**今日验收**：能看到模型返回的原始 JSON 结构，并能指出哪部分是内容、哪部分是元信息（token 数等）。

---

### Day 24（周三）· 密钥安全 + 环境变量

| 项 | 内容 |
|---|---|
| 任务 | 把密钥从代码里挪到环境变量 |
| 动作 | ①装 `python-dotenv` ②建 `.env` 文件放密钥 ③**建 `.gitignore` 把 `.env` 排除掉** ④代码里用 `os.getenv()` 读 |
| 关键 | **密钥永远走环境变量，仓库里只放一份说明文档。** 这是安全习惯，也是从业者基本素养 |
| 产出 | 代码里没有明文密钥，`.env` 不会被提交 |

**今日验收**：检查自己的 GitHub 仓库，确认**没有** `.env` 文件。

---

### Day 25（周四）· 连续对话循环

| 项 | 内容 |
|---|---|
| 任务 | 做一个能连续提多个问题的循环 |
| 动作 | ①用 `while` 循环让用户持续输入 ②每次把问题发给模型、打印回答 ③输入特定词（如 exit）退出 |
| 关键概念 | **多轮对话需要把历史消息一起发过去**。这是"记忆"的本质——模型本身不记得上一句 |
| 产出 | `week4_chat.py`，能连续对话 |

**今日验收**：能解释"为什么模型能记住上一句"（因为程序把历史一起发了）。

---

### Day 26（周五）· 异常处理三种情况

| 项 | 内容 |
|---|---|
| 任务 | 处理请求失败、超时、返回为空三种异常 |
| 动作 | ①用 `try/except` 包住请求 ②设置超时时间 ③返回空时给默认提示 ④失败时自动重试 1-2 次 |
| 产出 | 网络断了程序不崩，而是给出明确提示 |

**今日验收**：拔网线（或断网）跑一次，程序给出友好提示而不是崩溃。

---

### Day 27（周六）· 成本与性能统计

| 项 | 内容 |
|---|---|
| 任务 | 建立成本意识——统计每次请求的 token 和耗时 |
| 动作 | ①从返回结果里提取 token 数量 ②记录每次请求耗时 ③算"跑 100 条要花多少钱" |
| 为什么 | **客户最关心的两个指标就是成本和响应速度** |
| 产出 | `week4_stats.py`，每次请求后打印 token 数和耗时 |

**今日验收**：能报出"单次请求平均耗时 X 秒、平均消耗 Y token、跑 100 次大约 Z 元"。

---

### Day 28（周日）· 提交 + 周验收

**本周验收清单**

- [ ] 能在命令行连续提问并获得回答
- [ ] 代码里没有明文密钥，`.env` 未被提交
- [ ] 网络异常时程序不崩，有明确提示
- [ ] 能报出单次请求的平均耗时与成本
- [ ] 能说清"一次请求经过了哪些环节"

---

## 第 5 周 · 让模型稳定输出结构化结果

**本周目标**：把模型从"自由聊天"变成"程序可用的稳定输出"。这是"工程化"与"随便调调"的分界。

### Day 29（周一）· 结构化输出入门

| 项 | 内容 |
|---|---|
| 任务 | 让模型返回固定格式（JSON），包含规定字段 |
| 动作 | ①在提示词里明确要求返回 JSON，给出字段示例 ②用 Python 的 `json.loads()` 解析 ③打印解析后的字典 |
| 产出 | `week5_structured.py`，能拿到可解析的字典 |

**今日验收**：模型返回的内容能被 `json.loads()` 成功解析。

---

### Day 30（周二）· 日报审核实战

| 项 | 内容 |
|---|---|
| 任务 | 用真实业务场景练结构化输出 |
| 动作 | 把第 2 周写的"杂乱的日报文本"丢给模型，让它判断：①四项字段是否齐全 ②缺哪项 ③工作内容是否与昨日重复 ④按规定格式返回 |
| 产出 | `week5_report_review.py` |

**今日验收**：输入杂乱文本，输出结构化的审核结果字典。

---

### Day 31（周三）· 处理"模型不守规矩"

| 项 | 内容 |
|---|---|
| 任务 | 处理模型输出格式不稳定的问题 |
| 典型问题 | ①多说了几句话破坏 JSON ②字段名不对 ③不按数量要求返回 |
| 解法 | ①提示里给例子（few-shot）②程序校验返回值，不合格就重试 ③对关键字段做二次确认 ④用正则提取 JSON 部分 |
| 产出 | 一个健壮的解析函数，能处理各种不规矩的输出 |

**今日验收**：连续跑 10 次，成功率显著提升；失败时有明确处理而不是崩溃。

---

### Day 32（周四）· 重试机制

| 项 | 内容 |
|---|---|
| 任务 | 写一个通用的"调用 + 校验 + 重试"封装 |
| 动作 | ①写函数：调用模型 → 校验格式 → 不合格就带错误反馈重试 → 最多 3 次 ②记录每次重试的原因 ③3 次都失败就返回明确错误 |
| 产出 | `week5_retry.py`，可复用的健壮调用函数 |

**今日验收**：故意让模型返回错误格式，验证重试机制生效。

---

### Day 33（周五）· 成本优化初探

| 项 | 内容 |
|---|---|
| 任务 | 优化成本与延迟 |
| 动作 | ①对比长短提示词的 token 差异 ②尝试让模型只返回必要字段 ③记录优化前后的成本对比 |
| 产出 | 一份优化前后对比记录 |

**今日验收**：能说出"你是怎么把成本降下来的"。

---

### Day 34（周六）· 整合：日报审核服务

| 项 | 内容 |
|---|---|
| 任务 | 把第 5 周所学串成一个完整小服务 |
| 动作 | 读日报文件 → 逐条送模型审核 → 收集结果 → 输出结构化汇总 |
| 产出 | 一个能跑的服务脚本 |

**今日验收**：一条命令跑完全流程，输出格式稳定。

---

### Day 35（周日）· 提交 + 周验收

**本周验收清单**

- [ ] 同样输入连续跑 10 次，输出格式稳定可解析
- [ ] 程序能自动发现格式不合格并重试
- [ ] 能报出单次请求的平均耗时与成本
- [ ] 有成本优化的对比记录
- [ ] 能说出至少三种"模型不守规矩"的应对策略

---

## 第 6 周 · 让模型学会"用工具"

**本周目标**：引入 Function Calling（工具调用）。
**本周资源**：咕泡「08 应用篇 Agent」+ B站「Function calling 技术详解（先知，13集）」

### Day 36（周一）· 理解工具调用

| 项 | 内容 |
|---|---|
| 任务 | 理解核心概念：模型自己算不出"今天多少人交了日报"，但它可以决定"我需要去查一下数据库" |
| 动作 | ①看咕泡 08 篇对应章节 ②用一句话写清"工具调用"是什么 |
| 产出 | 一份概念笔记（3-5 句） |

**今日验收**：能给别人讲清工具调用和普通调用的区别。

---

### Day 37（周二）· 定义并实现第一个工具

| 项 | 内容 |
|---|---|
| 任务 | 写一个能被模型"调用"的函数（查日报数据） |
| 动作 | ①写函数 `query_reports(date)` ②按工具调用的格式描述它（名称、用途、参数） ③手动模拟一次完整流程 |
| 产出 | `week6_tools.py` |

**今日验收**：能手动走完"模型说要调用 → 我执行 → 把结果给模型 → 模型作答"的流程。

---

### Day 38（周三）· 接三个工具

| 项 | 内容 |
|---|---|
| 任务 | 给模型三个可用工具 |
| 工具清单 | ①查日报数据 ②算统计指标（如"这周谁没交"） ③读指定文件 |
| 动作 | 实现三个工具，并让模型的回答规则里包含它们的描述 |
| 产出 | 模型能根据问题自主选择调哪个工具 |

**今日验收**：问"这周谁没交日报"，模型能正确选择调用统计工具。

---

### Day 39（周四）· 程序控制流 ⭐ 本周技术核心

| 项 | 内容 |
|---|---|
| 任务 | 处理好模型与程序之间的控制流 |
| 要处理的情况 | ①模型说要用工具时程序怎么接 ②工具执行失败怎么告诉模型 ③模型连续要调用多个工具怎么处理 ④**出现死循环怎么打断**（设最大轮次） |
| 关键 | **本周重点不是提示词，是程序控制流** |
| 产出 | 一个带最大轮次保护的工具调用循环 |

**今日验收**：工具故意报错，模型能收到错误信息并给出合理回应；死循环有保护。

---

### Day 40（周五）· 多轮记忆

| 项 | 内容 |
|---|---|
| 任务 | 让对话有上下文记忆 |
| 动作 | ①维护消息历史列表 ②每轮把历史一起发送 ③设计"什么时候该忘掉早期内容"（避免上下文过长） |
| 产出 | 能接续上一句提问的问答 |

**今日验收**：问"这周谁没交"→ 接着问"那上周呢"，系统理解这是在延续话题。

---

### Day 41（周六）· 整合：日报收集+AI审核服务（简历第一个项目）⭐

| 项 | 内容 |
|---|---|
| 任务 | 把第 4-6 周成果合并成一个完整服务 |
| 功能 | 拉数据 → 模型审核 → 工具调用 → 生成汇总 → 输出结果 |
| 意义 | 这是一个完整的、有真实用户（我自己和团队）的服务。**会成为简历上的第一个技术项目** |
| 产出 | 完整服务代码 + README |

**今日验收**：服务能自动完成全流程，异常不中断。

---

### Day 42（周日）· 提交 + 周验收 + 阶段总结

**本周验收清单**

- [ ] 模型能自主选择合适的工具
- [ ] 工具调用有最大轮次保护，不会死循环
- [ ] 工具失败时模型能收到错误并合理回应
- [ ] 支持多轮对话记忆
- [ ] 整合服务能完整跑通
- [ ] 能说清这个服务每个模块在干什么

**阶段二总验收（第 4-6 周）**

自测问题：**如果模型返回的格式不符合要求，你会怎么处理？**
合格线：能说出至少三种策略（提示优化、程序校验重试、关键字段二次确认），并知道各自的代价。

---

## 第 7 周 · 文档变成可检索的块（RAG 开始）⭐

**本周目标**：把 SOP 文档处理成可检索的结构化块。
**本周资源**：咕泡「13 RAG 入门核心原理」+ B站「费曼学徒冬瓜 RAG全集」（先看这个建立直觉）+「卢菁博士 RAG」（打通原理）

### Day 43（周一）· RAG 概念 + 读文档

| 项 | 内容 |
|---|---|
| 任务 | ①理解 RAG 解决什么问题（**模型不知道我们的内部资料**）②把第一份 SOP 文档读进来 |
| 动作 | ①看冬瓜的 RAG 入门 ②用 Python 读取一份 SOP 文档（txt/word/pdf） |
| 产出 | 能把文档内容打印出来 |

**今日验收**：能用一句话解释"RAG 解决了什么问题"。

---

### Day 44（周二）· 批量读取 + 清洗

| 项 | 内容 |
|---|---|
| 任务 | 批量处理全部 SOP 文档，并清洗掉无意义内容 |
| 清洗对象 | 页眉页脚、重复标注、乱码、多余空行 |
| 动作 | ①遍历目录读所有文档 ②写清洗函数 ③处理格式不一致的情况（不同文档结构不同） |
| 产出 | 清洗后的纯文本 |

**今日验收**：能批量处理全部文档不中断；清洗后的文本读起来是干净的。

---

### Day 45（周三）· 文本切分（chunking）⭐

| 项 | 内容 |
|---|---|
| 任务 | 把长文档切成小段 |
| 参数标准 | ①精确事实类内容切 **256-512 token** ②叙述类内容切 **512-1024 token** ③相邻块保留 **10%-20% 重叠**（避免关键信息正好被切断） |
| 动作 | ①先实现固定长度切分 ②理解 overlap 的作用 |
| 产出 | `week7_chunk.py`，能切出合理大小的块 |

**今日验收**：切出来的块大小分布合理，**不出现一两百字的碎块或三千字的巨块**。

---

### Day 46（周四）· 语义切分

| 项 | 内容 |
|---|---|
| 任务 | 从"按字数切"升级到"按语义边界切" |
| 做法 | 当相邻内容的语义相似度明显下降时，在那里断开 |
| 动作 | ①计算相邻句子的相似度 ②在相似度低谷处断开 ③对比固定切分和语义切分的差别 |
| 产出 | 语义切分函数 |

**今日验收**：能说出"语义切分比固定切分好在哪"，并用实际例子说明。

---

### Day 47（周五）· 按标题层级结构化切分

| 项 | 内容 |
|---|---|
| 任务 | 利用文档的标题结构切分（比纯文本切分更优） |
| 动作 | ①识别文档的标题层级（一、/（一）/1.） ②按章节切块 ③过长的章节再二次切分 |
| 产出 | 结构化的块列表 |

**今日验收**：每个块对应文档里一个明确的章节，能追溯。

---

### Day 48（周六）· 标注来源 + 落盘

| 项 | 内容 |
|---|---|
| 任务 | 给每块标注来源信息并存成结构化文件 |
| 标注内容 | 来自哪份文档、哪一节、原文起止位置 |
| 动作 | ①把块存成 JSON（含内容和元信息）②写一个函数能根据块查回原文位置 |
| 产出 | `chunks.json`，每块都可追溯 |

**今日验收**：随机抽一个块，能准确说出它来自哪份文档的哪一节。

---

### Day 49（周日）· 提交 + 周验收

**本周验收清单**

- [ ] 能批量处理全部 SOP 文档
- [ ] 清洗后的文本无页眉页脚乱码
- [ ] 切块大小分布合理（无碎块无巨块）
- [ ] 实现了语义切分，能说出它比固定切分好在哪
- [ ] 每块都能追溯到原始来源
- [ ] `chunks.json` 结构清晰

> **提醒**：这一步看起来不涉及"AI"，但它决定了后面系统的上限。**检索不到正确内容，模型再强也答不对。**

---

## 第 8 周 · 向量化与检索

**本周目标**：实现完整的"提问→检索→交给模型→获得回答"流程，并做优化。
**本周资源**：咕泡「13 RAG 篇」+ B站「吴恩达 RAG 五大项目」+「黑马程序员 RAG与Agent实战」

### Day 50（周一）· 向量化基础

| 项 | 内容 |
|---|---|
| 任务 | 把文本块转成向量 |
| 动作 | ①理解"向量就是一组数字，语义相近的文本向量也相近" ②选一个向量模型 ③把第 7 周的块全部转向量 |
| 产出 | 每块对应的向量 |

**今日验收**：能解释"为什么向量能表示语义"。

---

### Day 51（周二）· 建索引 + 向量库

| 项 | 内容 |
|---|---|
| 任务 | 把向量存进向量库并建索引 |
| 动作 | ①选一个本地能跑的轻量向量库 ②把向量和元信息一起存进去 ③测试按向量查询 |
| 产出 | 可查询的向量库 |

**今日验收**：给一个问题，能从库里查出最相似的 3 个块。

---

### Day 52（周三）· 完整 RAG 流程打通

| 项 | 内容 |
|---|---|
| 任务 | 实现端到端：提问 → 检索 → 交给模型 → 获得回答 |
| 动作 | ①检索 top-k 片段 ②把片段和问题一起构造提示词 ③让模型基于片段回答 ④**要求模型附上引用来源** |
| 产出 | `week8_rag.py`，第一个能用的问答系统 |

**今日验收**：能对 SOP 内容提出问题并得到有依据的回答。

---

### Day 53（周四）· 检索优化：混合检索

| 项 | 内容 |
|---|---|
| 任务 | 解决纯向量检索的短板 |
| 问题 | "试听课积分怎么算"这种关键词明确的查询，向量检索可能不如关键词匹配准 |
| 解法 | **混合检索**——同时跑关键词匹配（BM25）和向量检索，把两路结果融合 |
| 动作 | ①实现关键词检索 ②实现结果融合（RRF 融合) ③对比效果 |
| 产出 | 混合检索函数 |

**今日验收**：对比纯向量 vs 混合检索，能说出差异在哪类问题上。

---

### Day 54（周五）· 重排（reranking）⭐

| 项 | 内容 |
|---|---|
| 任务 | 加一层精排，提升准确率 |
| 做法 | ①先粗筛**召回 20-50 个**候选 ②用更精细的重排模型重新打分 ③**取前 3-5 个**交给大模型 |
| 动作 | ①接入重排模型 ②实现两阶段检索流程 ③做加/不加对比实验 |
| 产出 | 带重排的检索流程 |

**今日验收**：有加/不加对比数据，能说清差在哪里。

---

### Day 55（周六）· 引用与溯源

| 项 | 内容 |
|---|---|
| 任务 | 让回答能追溯到来源 |
| 动作 | ①回答里标注用了哪几个片段 ②显示每个片段的来源文档和章节 ③处理"引用错误"的情况 |
| 产出 | 带完整引用的回答 |

**今日验收**：每个回答都能看到依据来自哪份文档的哪一节。

---

### Day 56（周日）· 提交 + 周验收

**本周验收清单**

- [ ] 系统能基于全部 SOP 内容回答问题
- [ ] 回答里能附上引用来源
- [ ] 实现了混合检索，能说出它解决了什么问题
- [ ] 实现了重排，并有加/不加的对比数据
- [ ] 能说出"检索不准时先调哪个环节"及理由

---

## 第 9 周 · 从"能答"到"答得可靠"（评估周）⭐

**本周目标**：做评估。**这是把业余项目和专业项目分开的分水岭。**
**本周资源**：咕泡「13 RAG 篇」评估部分 + 行业评估指标资料

### Day 57（周一）· 整理评估问题集

| 项 | 内容 |
|---|---|
| 任务 | 整理 20 个真实问题 |
| 要求 | 从一线教练实际会问的问题里选，**必须包含**：简单的、模糊的、多条件的、文档里没有答案的 |
| 动作 | ①翻聊天记录找真实问题 ②补齐到 20 条 ③分类标注 |
| 产出 | `eval_questions.md`，20 个问题 |

**今日验收**：20 个问题都有真实来源，不是编的。

---

### Day 58（周二）· 人工判定

| 项 | 内容 |
|---|---|
| 任务 | 逐个测试并人工判定对错 |
| 动作 | ①20 个问题逐个输入系统 ②记录回答 ③人工判定：答对了/答错了/答不全/编造 |
| 产出 | `eval_results.md`，20 条完整记录 |

**今日验收**：20 个问题都有系统回答 + 人工判定结果。

---

### Day 59（周三）· 计算四项核心指标

| 项 | 内容 |
|---|---|
| 任务 | 计算行业标准的 RAG 评估指标 |
| 四项指标 | ①**忠实度**（回答里有事实错误的占比，要降到很低）②**答案相关性**（答案是否切题）③**上下文精确率**（检索出来的内容里有多少真正有用）④**上下文召回率**（应该被检索到的有多少真的被检索到了） |
| 动作 | ①先手工算一遍理解含义 ②再了解有成熟工具支持（如 RAGAS）③算出自已的分数 |
| 产出 | 评估报告，含四项指标 |

**今日验收**：能说出四项指标各自的含义，并报出自己的分数。

---

### Day 60（周四）· "答不上来"的处理 ⭐ 安全底线

| 项 | 内容 |
|---|---|
| 任务 | 处理检索不到内容的情况 |
| 核心要求 | 系统检索不到相关内容时，**必须明确说"资料中没有这个信息"**，而不是用模型自己的知识编一个听起来合理的答案 |
| 动作 | ①设置相似度阈值 ②低于阈值时返回"不知道" ③测试那条"文档里没有答案"的问题 |
| 关键 | **伪造答案比答不出来危险得多。** 这是能不能作为求职作品的分水岭 |

**今日验收**：问一个文档里根本没有的问题，系统明确说"资料中没有这个信息"。

---

### Day 61（周五）· 针对性改进 + 对比

| 项 | 内容 |
|---|---|
| 任务 | 针对表现差的问题做一轮改进 |
| 动作 | ①找出答得最差的那一类问题 ②分析原因（切分？检索？提示词？）③做针对性改进 ④重新跑评估，做前后对比 |
| 产出 | 改进前后对比数据 |

**今日验收**：能明确指出"哪类问题表现差、为什么、我做了什么改进、提升了多少"。

---

### Day 62（周六）· 多轮对话 + 工具兜底

| 项 | 内容 |
|---|---|
| 任务 | 补齐两项能力 |
| 动作 | ①多轮对话：问"那正式课呢"，系统要知道这是在接着上一个话题 ②工具兜底：查数据失败时给出明确提示，不静默返回空 |
| 产出 | 支持多轮 + 有兜底 |

**今日验收**：多轮追问能正确理解；工具失败有明确提示。

---

### Day 63（周日）· 提交 + 周验收 + 阶段总结

**本周验收清单**

- [ ] 20 个问题的评估结果有记录、有分析
- [ ] 算出四项核心指标（忠实度/答案相关性/上下文精确率/上下文召回率）
- [ ] 检索不到时会明确说"资料中没有"，不编造
- [ ] 对差的问题做过至少一轮针对性改进，有对比数据
- [ ] 支持多轮对话
- [ ] 工具有兜底策略

**阶段三总验收（第 7-9 周）**

自测问题：**用一句话解释 RAG 解决了什么问题？检索不准时你会先调哪个环节？**
合格线：能解释"模型不知道私有数据"这一核心问题；能按**切分→检索→重排**的顺序定位瓶颈并说明理由。

---

## 第 10 周 · 容器化与部署

**本周目标**：打包成"在任何装了 Docker 的机器上一条命令就能跑起来"。
**本周资源**：咕泡「09 部署篇」+ B站「技术胖 Docker」

### Day 64（周一）· 装 Docker + 跑通第一个容器

| 项 | 内容 |
|---|---|
| 任务 | ①装 Docker ②跑通一个最简单的容器（如 hello-world）③理解"镜像"和"容器"的区别 |
| 产出 | Docker 环境可用 |

**今日验收**：能说出"镜像"和"容器"的区别。

---

### Day 65（周二）· 写 Dockerfile

| 项 | 内容 |
|---|---|
| 任务 | 给 RAG 项目写容器配置文件 |
| 动作 | ①选基础镜像 ②复制代码 ③安装依赖 ④设置启动命令 |
| 产出 | `Dockerfile`，能构建成功 |

**今日验收**：`docker build` 成功。

---

### Day 66（周三）· 解决"在我电脑上是好的"

| 项 | 内容 |
|---|---|
| 任务 | 处理容器化过程中的典型问题 |
| 典型问题 | ①文件路径（**容器里没有你电脑上的那些盘符**）②依赖版本不一致 ③编码问题 ④端口冲突 |
| 关键 | 解决这些问题的过程就是真正在学部署——**不是记住几条 Docker 命令，而是建立"运行环境是一个需要被明确定义的东西"这个认知** |
| 产出 | 容器里能跑通 |

**今日验收**：容器内跑通全流程，且你能说出遇到并解决了哪几个问题。

---

### Day 67（周四）· 密钥与配置剥离

| 项 | 内容 |
|---|---|
| 任务 | 把密钥和配置从代码里完全剥离 |
| 动作 | ①用环境变量传入密钥（`docker run -e`）②配置文件挂载进容器 ③确认镜像里**不包含**任何密钥 |
| 产出 | 安全的镜像 |

**今日验收**：检查镜像内容，确认无密钥泄露。

---

### Day 68（周五）· 依赖固定

| 项 | 内容 |
|---|---|
| 任务 | 把依赖固定进镜像 |
| 动作 | ①用 `requirements.txt` 锁定版本 ②测试从零构建 ③解决版本冲突 |
| 产出 | 可重现的构建 |

**今日验收**：删掉本地缓存重新构建，仍能成功。

---

### Day 69（周六）· 干净环境测试 ⭐

| 项 | 内容 |
|---|---|
| 任务 | 把镜像发给一个同事，让他在自己电脑上跑 |
| 动作 | ①导出镜像 ②写完整的部署说明 ③观察他能否一条命令跑起来 |
| 产出 | 可交付的镜像 + 部署文档 |

**今日验收**：同事在他的电脑上能一条命令跑起来。

---

### Day 70（周日）· 提交 + 周验收

**本周验收清单**

- [ ] Docker 环境可用，理解镜像与容器的区别
- [ ] Dockerfile 能构建成功
- [ ] 容器内能跑通全流程
- [ ] 镜像里不含任何密钥
- [ ] 依赖固定，可重现构建
- [ ] **把镜像发给同事，他在自己电脑上能一条命令跑起来**
- [ ] README 有完整的部署说明

---

## 第 11 周 · 接入真实场景，收反馈

**本周目标**：让真实用户用起来，收集反馈并改进。
**本周资源**：开始看 **FDE 求职资料包** + 咕泡「07 产品篇」

### Day 71（周一）· 准备接入方式

| 项 | 内容 |
|---|---|
| 任务 | 把系统接到教练实际会用的地方 |
| 动作 | ①选接入方式（飞书群机器人 / 简单的网页界面 / 命令行）②实现最简可用版本 |
| 产出 | 教练能访问的入口 |

**今日验收**：能通过选定的方式访问到系统。

---

### Day 72（周二）· 设计反馈收集表 ⭐

| 项 | 内容 |
|---|---|
| 任务 | 准备好"怎么收反馈" |
| 关键 | **不能只问"好用吗"**——要记录：①问了什么问题 ②系统答了什么 ③教练认为对不对 ④教练最终有没有用上这个答案 |
| 产出 | 反馈记录表 |

**今日验收**：反馈表的字段设计完成，且你能说出"为什么要记这四项"。

---

### Day 73（周三）· 找五位教练试用

| 项 | 内容 |
|---|---|
| 任务 | 找五位一线教练开始试用 |
| 动作 | ①招募 5 位真实用户 ②说明使用方式 ③提醒记录问题 |
| 产出 | 5 位试用用户 |

**今日验收**：5 位教练已开始试用。

---

### Day 74（周四）· 收集使用记录

| 项 | 内容 |
|---|---|
| 任务 | 收集真实提问与判定记录 |
| 动作 | ①跟踪他们的使用 ②收集提问和回答 ③请他们判定对错 |
| 产出 | 真实使用记录 |

**今日验收**：至少有 5 条完整的"提问-回答-判定"记录。

---

### Day 75（周五）· 分析反馈 + 改进

| 项 | 内容 |
|---|---|
| 任务 | 从反馈中找出最严重的问题并处理掉一类 |
| 动作 | ①归类错误（检索错？切分错？提示词问题？）②找出最严重的一类 ③做针对性改进 ④验证改进效果 |
| 底线 | **系统答错的每一类问题都要记下来，一周内至少处理掉其中最严重的一类** |
| 产出 | 改进记录 |

**今日验收**：至少完成一轮基于真实反馈的改进，并有效果验证。

---

### Day 76（周六）· 汇总量化结果

| 项 | 内容 |
|---|---|
| 任务 | 算清楚项目的量化价值 |
| 指标 | ①一线教练答疑工作量减少了多少 ②答对率是多少 ③平均响应时间多久 ④单次问答成本多少 |
| 产出 | 量化指标表 |

**今日验收**：四个指标都有数字。

---

### Day 77（周日）· 提交 + 周验收

**本周验收清单**

- [ ] 系统接入到教练能用的地方
- [ ] 反馈收集表设计完整（四个字段）
- [ ] 五位真实用户完成试用
- [ ] 有完整的提问与判定记录
- [ ] 至少完成一轮基于真实反馈的改进
- [ ] 四个量化指标都有数字

---

## 第 12 周 · 整理成作品，复盘收尾

**本周目标**：把项目整理成能被别人看懂的材料，并做 12 周复盘。
**本周资源**：**FDE 求职资料包（重点用）** + 咕泡「07 产品篇」

### Day 78（周一）· 项目说明文档（上）

| 项 | 内容 |
|---|---|
| 任务 | 按 FDE 作品集标准整理项目 |
| 必须包含 | ①最初要解决什么问题、谁决定要做 ②当时缺哪些信息、我通过什么方式搞清楚需求 ③系统架构长什么样、做了哪些关键权衡 |
| 关键 | **FDE 的作品集应该"解释交付过程，而不只是展示完成的界面"** |

**今日验收**：前三部分写完。

---

### Day 79（周二）· 项目说明文档（下）

| 项 | 内容 |
|---|---|
| 任务 | 补齐剩余部分 |
| 包含 | ④数据从哪来 ⑤考虑了哪些安全和权限问题 ⑥效果提升了多少 ⑦哪些模块可以复用到别的地方 |
| 产出 | 完整的项目说明文档 |

**今日验收**：七部分齐全。

---

### Day 80（周三）· 简历改写 ⭐

| 项 | 内容 |
|---|---|
| 任务 | 把项目写成简历条目 |
| 公式 | **约束 + 可衡量的改变 + 安全机制**，不是工具清单 |
| 反例 | `参与 LLM 聊天机器人` |
| 正例 | `设计并部署了基于 RAG 的知识助手，带来源引用、权限控制，以及 20 个真实问题的评估数据集，将答案采纳率从 X% 提升到 Y%` |
| 产出 | 简历上的 1-2 条项目描述 |

**今日验收**：简历条目里有数字、有约束、有安全机制。

---

### Day 81（周四）· 二十分钟讲述练习

| 项 | 内容 |
|---|---|
| 任务 | 能在 20 分钟内讲清整个项目从需求到落地的全过程 |
| 动作 | ①写讲述提纲 ②**录下来自己看** ③检查有没有"架构优先"的句子（先讲技术不讲问题的都要改） |
| 产出 | 讲述提纲 + 一次录音 |

**今日验收**：能连续讲 20 分钟不断线，且**先讲问题、再讲方案**。

---

### Day 82（周五）· 案例面试练习（CIRCLES）

| 项 | 内容 |
|---|---|
| 任务 | 用 CIRCLES 框架练 3 个案例题 |
| 案例题 | ①"一所学校想用 AI 减少老师备课时间，你做什么？" ②"一家机构有 100 位教练，想用 AI 自动回答学员问题，你怎么做？" ③"一个客户想用 AI 分析家长反馈，设计一个方案" |
| 关键 | **先花 5 分钟问问题**，把问题定义说出口，划出一个有指标、有"放弃标准"的最小楔子，然后才开始设计 |
| 产出 | 3 份案例答题记录（**要录音**） |

**今日验收**：3 个案例都先问问题、再定义问题、最后给方案；有录音回看。

---

### Day 83（周六）· 12 周总复盘

| 项 | 内容 |
|---|---|
| 任务 | 回答四个问题 |
| 问题 | ①12 周里哪些方法有效 ②哪些走了弯路 ③技术能力到了什么水平 ④下一步缺什么 |
| 额外 | 回顾 `踩坑记录.md`，把反复出现的问题写成自己的检查清单 |
| 产出 | 复盘文档 |

**今日验收**：四个问题都有明确回答。

---

### Day 84（周日）· 结营 + 下一步规划

| 项 | 内容 |
|---|---|
| 任务 | 总结 12 周成果，规划下一步 |
| 成果清单 | ①四个可运行的产品 ②一份项目说明文档 ③一份评估报告 ④一份简历上的项目条目 |

**12 周总验收清单**

- [ ] 每个阶段都有一个能运行的产物
- [ ] 项目包含完整的评估环节，能说清"问了 20 个问题、答对几个、错的是哪几类、我做了什么改进"
- [ ] 能在 20 分钟内讲清整个项目
- [ ] 简历上有 1-2 条带数字的项目描述
- [ ] 练过 3 个以上 CIRCLES 案例
- [ ] 能回答"如果重做一次会怎么改"

**自测问题**：**这个系统的答对率是多少？哪些问题答不好？为什么？**
合格线：有完整评估数据；能明确指出现有局限；能说出下一步改进方向。

---

## 附录：三条贯穿全程的验收底线

1. **每个阶段结束必须有一个能运行的产物**。不接受"看完了但没做出来"。视频看完不算完成，代码跑起来才算。
2. **项目必须包含评估环节**。只会回答问题的系统不算作品。
3. **能向非技术的人讲明白**。任何一个技术点，如果用不涉及术语的话讲不清，说明我自己没真懂。
